name: vertebrates
description: SDRF schema for vertebrate samples
extends: default
min_columns: 7
fields:
- name: characteristics_developmental_stage
  sdrf_name: characteristics[developmental stage]
  description: Developmental stage
  requirement: optional
  validators:
  - type: whitespace
    params: {}
